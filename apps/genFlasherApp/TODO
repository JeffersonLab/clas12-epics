
--  2 detectors will be using same board
    -- they want individual screens
    -- thought that would be easy, but current PV naming scheme makes it not 
    ---- ideally single opi for one channel, with macros to define channel
    
-- save/restore
    -- should be per channel and not per board
    -- why not use burt instead of single-use caget/caput scripts?
    -- remove dependence on zenity

-- rename to jlabFlasher/jflasher (ala jscaler)?



