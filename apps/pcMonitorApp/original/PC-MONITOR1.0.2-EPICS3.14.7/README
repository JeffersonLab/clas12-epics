The directory "pcMonitorApp" contains the PC monitoring project
Before you start set the variables in the setup file and type 
the commands as following:

set in the configure/RELEASE
EPICS_BASE=/usr/local/epics/base
. ./setup
cd pcMonitorApp/src
less README.txt

pcMonitorApp/src/Makefile
should contain PROD_HOST = pcMonitor
instead of     PROD_IOC  = pcMonitor 
